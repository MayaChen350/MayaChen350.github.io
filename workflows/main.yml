name: Minimal Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print API Key
      env: 
          last_fm_api: ${{ secrets.LAST_FM_API_KEY }}"
